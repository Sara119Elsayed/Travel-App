import 'package:flutter/material.dart';
class ActivitiesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('الأنشطة'),
      ),
      body: Center(
        child: Text('هذه صفحة الأنشطة'),
      ),
    );
  }
}
