import 'package:flutter/material.dart';
class NearbyPlacesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('الأماكن القريبة'),
      ),
      body: Center(
        child: Text('هذه صفحة الأماكن القريبة'),
      ),
    );
  }
}